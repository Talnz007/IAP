name: Feature Pipeline

on:
  schedule:
    # Run every hour
    - cron: '0 * * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  feature-extraction:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run feature pipeline
        env:
          AQI_API_KEY: ${{ secrets.AQI_API_KEY }}
          OPENWEATHERMAP_API_KEY: ${{ secrets.OPENWEATHERMAP_API_KEY }}
          HOPSWORKS_API_KEY: ${{ secrets.HOPSWORKS_API_KEY }}
          HOPSWORKS_PROJECT_NAME: ${{ secrets.HOPSWORKS_PROJECT_NAME }}
        run: |
          echo "Checking secrets..."
          echo "OPENWEATHERMAP_API_KEY length: ${#OPENWEATHERMAP_API_KEY}"
          echo "HOPSWORKS_API_KEY length: ${#HOPSWORKS_API_KEY}"
          export OPENWEATHERMAP_API_KEY="${OPENWEATHERMAP_API_KEY}"
          export HOPSWORKS_API_KEY="${HOPSWORKS_API_KEY}"
          export HOPSWORKS_PROJECT_NAME="${HOPSWORKS_PROJECT_NAME}"
          python -c "import os; print('Python sees OPENWEATHERMAP_API_KEY:', bool(os.getenv('OPENWEATHERMAP_API_KEY')))"
          python pipelines/feature_pipeline.py
      
      - name: Upload logs
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: feature-pipeline-logs
          path: logs/
          retention-days: 7
